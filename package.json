{
  "name": "extracker",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "nx run web:dev",
    "build": "nx run web:build",
    "start": "nx run web:start",
    "lint": "nx run web:lint",
    "db:test": "nx run web:db:test",
    "db:verify": "nx run web:db:verify",
    "prisma:generate": "nx run web:prisma:generate",
    "prisma:push": "nx run web:prisma:push",
    "prisma:studio": "nx run web:prisma:studio",
    "prisma:seed": "nx run web:prisma:seed",
    "seed:notifications": "nx run web:seed:notifications"
  },
  "devDependencies": {
    "nx": "^20.4.0",
    "@nx/next": "^20.4.0",
    "@nx/workspace": "^20.4.0"
  }
}
