You are an expert Next.js + Framer Motion developer and UI/UX designer.
Your task is to design and code a complete Expenses Management Page that allows users to view, create, edit, and delete expenses.

🧩 Page Features & Requirements:

Display All Expenses:

Show a dynamic list or table of expenses (date, category, amount, description).

Include a filter or search bar to quickly find specific expenses.

Each expense row should have Edit and Delete action buttons.

Use Framer Motion for smooth item transitions (e.g., when adding, editing, or removing).

Create Expense:

Provide a form with inputs:

Expense Name

Amount

Category (dropdown)

Date

Notes (optional)

Include a “Add Expense” button that triggers a motion pop-up or slide-in modal.

Animate form entry and submission success using Framer Motion.

Edit Expense:

When clicking Edit, open a pre-filled form (modal or inline edit mode).

Animate with slide or fade transitions for a smooth user experience.

Ensure data updates dynamically without full reload.

Delete Expense:

Include a confirmation modal before deleting.

Use a subtle fade-out animation on delete.

UI/UX & Design:

Clean, modern dashboard style layout.

Use consistent spacing, typography, and accent colors (e.g., teal + white).

All transitions and hover effects handled by Framer Motion.

Responsive design for desktop and mobile.

⚙️ Tech Stack:

React (functional components + hooks)

Framer Motion for all animations and transitions

Tailwind CSS Shadcn/ui for styling

Example state management with zustand if required (optional)

🧠 Deliverables:

Full React component code for the Expenses Page

CRUD (Create, Read, Update, Delete) logic examples

Framer Motion code snippets for:

Modal animation

List item transition

Button hover effect

Page load fade-in